#include <bits/stdc++.h>

using namespace std;

class BoardUnit
{
public:
	int color;	// White -> 1; Black ->2
	int kind;	//1=flatstone,2=wall,3=capstone

public:
	BoardUnit();
	BoardUnit(int bucolor,int bukind);
	// ~BoardUnit();

	/* data */
};